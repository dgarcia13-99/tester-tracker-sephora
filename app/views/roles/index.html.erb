<header>
  <h1>Roles</h1>
</header>

<section id="roles">
  <% @roles.each do |role| %>
    <%= render role %>
    <p>
      <%= link_to "Show this role", role %>
    </p>
  <% end %>
</section>

<%= link_to "New role", new_role_path %>
