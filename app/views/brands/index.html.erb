<header>
  <h1>Brands</h1>
</header>

<section id="brands">
  <% @brands.each do |brand| %>
    <%= render brand %>
    <p>
      <%= link_to "Show this brand", brand %>
    </p>
  <% end %>
</section>

<%= link_to "New brand", new_brand_path %>
